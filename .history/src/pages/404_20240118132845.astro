---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Error 404`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary">404</span>
        </h2>
        <h3 class="mb-8 font-bold text-3xl md:text-3xl">
          ¡Página no encontrada!
        </h3>
        <p class="text-3xl font-semibold md:text-3xl">Lo sentimos. La página que busca no está disponible.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Sin embargo, no hay motivo de preocupación. Le invitamos a descubrir una amplia variedad de contenidos interesantes en nuestra página principal.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Ir a la p&aacute;gina principal</a>
      </div>
    </div>
  </section>
</Layout>
